# move2reycle
A python script that moves all files with a specified extension/s to recycle bin (so that they can be restored if needed) from provided directory or list of directories and it's subfolders

To use the script you need to install send2trash. Install it using `pip install send2trash`
